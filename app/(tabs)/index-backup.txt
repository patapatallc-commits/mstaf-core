import { View, Image, StyleSheet, TouchableOpacity, Linking } from "react-native";

const BOT_URL = "https://wa.me/18622306637?text=Hello";
const UPLOAD_URL = "https://mstaf-core-1.onrender.com/upload.html";

export default function HomeScreen() {
  return (
    <View style={styles.container}>
      <Image
        source={require("../../assets/images/printomatic-landing.png")}
        style={styles.image}
        resizeMode="contain"
      />

      <TouchableOpacity style={styles.topUpload} onPress={() => Linking.openURL(UPLOAD_URL)} />
      <TouchableOpacity style={styles.topWhatsapp} onPress={() => Linking.openURL(BOT_URL)} />

      <TouchableOpacity style={styles.bottomUpload} onPress={() => Linking.openURL(UPLOAD_URL)} />
      <TouchableOpacity style={styles.bottomLaminate} onPress={() => Linking.openURL(BOT_URL)} />
      <TouchableOpacity style={styles.bottomImage} onPress={() => Linking.openURL(BOT_URL)} />
      <TouchableOpacity style={styles.bottomVideo} onPress={() => Linking.openURL(BOT_URL)} />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000",
  },

  image: {
    width: "100%",
    height: "100%",
    position: "absolute",
  },

  topUpload: {
    position: "absolute",
    top: "35%",
    left: "10%",
    width: "40%",
    height: "18%",
    zIndex: 1000,
  },

  topWhatsapp: {
    position: "absolute",
    top: "35%",
    left: "50%",
    width: "45%",
    height: "18%",
    zIndex: 1000,
  },

  bottomUpload: {
    position: "absolute",
    top: "55%",
    left: "0%",
    width: "25%",
    height: "45%",
    zIndex: 1000,
  },

  bottomLaminate: {
    position: "absolute",
    top: "55%",
    left: "25%",
    width: "25%",
    height: "45%",
    zIndex: 1000,
  },

  bottomImage: {
    position: "absolute",
    top: "55%",
    left: "50%",
    width: "25%",
    height: "45%",
    zIndex: 1000,
  },

  bottomVideo: {
    position: "absolute",
    top: "55%",
    left: "75%",
    width: "25%",
    height: "45%",
    zIndex: 1000,
  },
});